<button
    open-type="getUserInfo"
    bindgetuserinfo="onGetUserInfo"
    class="btn-primary btn-size-circle-88"
    loading="{{loading}}"
    disabled="{{loading || !loginRes.code ? true : false}}"
>绑定微信</button>