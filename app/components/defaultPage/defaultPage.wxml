<!--components/defaultPage/defaultPage.wxml-->

<view class="default-page">
  <image src="{{ default.imgSrc }}" class="icon"></image>
  <text class="title">{{ loading ? '加载中...' : default.title }}</text>
</view>
