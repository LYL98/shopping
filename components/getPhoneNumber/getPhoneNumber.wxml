<block wx:if="{{fromPage === 'login'}}">
  <button class="btn-primary btn-size-circle-88" open-type="getPhoneNumber" bindgetphonenumber="getPhoneNumber" loading="{{loading}}" disabled="{{loading}}">授权微信手机号</button>
</block>